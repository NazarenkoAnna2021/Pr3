import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    countArea: {
      display: 'flex',
      flexDirection: 'row',
      justifyContent:'space-around',
      alignItems: 'center',
      marginBottom: 250,
    },
    output: {
        fontSize: 100,
        width: 250,
        height: 110,
        textAlign: 'center'
    }
  });